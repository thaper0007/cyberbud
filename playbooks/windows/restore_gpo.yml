---
- name: Restore GPO from backup
  hosts: "{{ target_host }}"
  gather_facts: no

  tasks:
    - name: Restore GPO from selected backup
      ansible.windows.win_command: >
        powershell.exe -Command "Import-GPO -All -Path 'C:\\GPO_Backups\\{{ backup_folder }}'"
